The solution contains two scripts. The script "solution.py" contains the domain classes of the
problem and solves the use case given by the files "trucks.csv" and "cargo.csv". The script
"test_solution.py" tests the main functionalities of the solution.
